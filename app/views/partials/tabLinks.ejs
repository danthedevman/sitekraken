<%
  const tabs = [
    { key: 'chatbot', label: 'Chatbot', href: `/workspaces/${workspace._id}/chatbot` },
    { key: 'files', label: 'Files', href: `/workspaces/${workspace._id}/chatbot/files` },
    { key: 'knowledge', label: 'Knowledge', href: `/workspaces/${workspace._id}/chatbot/knowledge` }
  ];
%>

<ul class="nav nav-tabs mb-4">
  <% tabs.forEach(tab => { %>
    <li class="nav-item" role="presentation">
      <a
        href="<%= tab.href %>"
        class="nav-link <%= activeTab === tab.key ? 'active' : '' %>">
        <%= tab.label %>
      </a>
    </li>
  <% }) %>
</ul>
